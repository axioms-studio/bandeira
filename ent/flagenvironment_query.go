// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"database/sql/driver"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/felipekafuri/bandeira/ent/environment"
	"github.com/felipekafuri/bandeira/ent/flag"
	"github.com/felipekafuri/bandeira/ent/flagenvironment"
	"github.com/felipekafuri/bandeira/ent/predicate"
	"github.com/felipekafuri/bandeira/ent/strategy"
)

// FlagEnvironmentQuery is the builder for querying FlagEnvironment entities.
type FlagEnvironmentQuery struct {
	config
	ctx             *QueryContext
	order           []flagenvironment.OrderOption
	inters          []Interceptor
	predicates      []predicate.FlagEnvironment
	withFlag        *FlagQuery
	withEnvironment *EnvironmentQuery
	withStrategies  *StrategyQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the FlagEnvironmentQuery builder.
func (_q *FlagEnvironmentQuery) Where(ps ...predicate.FlagEnvironment) *FlagEnvironmentQuery {
	_q.predicates = append(_q.predicates, ps...)
	return _q
}

// Limit the number of records to be returned by this query.
func (_q *FlagEnvironmentQuery) Limit(limit int) *FlagEnvironmentQuery {
	_q.ctx.Limit = &limit
	return _q
}

// Offset to start from.
func (_q *FlagEnvironmentQuery) Offset(offset int) *FlagEnvironmentQuery {
	_q.ctx.Offset = &offset
	return _q
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (_q *FlagEnvironmentQuery) Unique(unique bool) *FlagEnvironmentQuery {
	_q.ctx.Unique = &unique
	return _q
}

// Order specifies how the records should be ordered.
func (_q *FlagEnvironmentQuery) Order(o ...flagenvironment.OrderOption) *FlagEnvironmentQuery {
	_q.order = append(_q.order, o...)
	return _q
}

// QueryFlag chains the current query on the "flag" edge.
func (_q *FlagEnvironmentQuery) QueryFlag() *FlagQuery {
	query := (&FlagClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(flagenvironment.Table, flagenvironment.FieldID, selector),
			sqlgraph.To(flag.Table, flag.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, flagenvironment.FlagTable, flagenvironment.FlagColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryEnvironment chains the current query on the "environment" edge.
func (_q *FlagEnvironmentQuery) QueryEnvironment() *EnvironmentQuery {
	query := (&EnvironmentClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(flagenvironment.Table, flagenvironment.FieldID, selector),
			sqlgraph.To(environment.Table, environment.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, flagenvironment.EnvironmentTable, flagenvironment.EnvironmentColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryStrategies chains the current query on the "strategies" edge.
func (_q *FlagEnvironmentQuery) QueryStrategies() *StrategyQuery {
	query := (&StrategyClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(flagenvironment.Table, flagenvironment.FieldID, selector),
			sqlgraph.To(strategy.Table, strategy.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, flagenvironment.StrategiesTable, flagenvironment.StrategiesColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first FlagEnvironment entity from the query.
// Returns a *NotFoundError when no FlagEnvironment was found.
func (_q *FlagEnvironmentQuery) First(ctx context.Context) (*FlagEnvironment, error) {
	nodes, err := _q.Limit(1).All(setContextOp(ctx, _q.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{flagenvironment.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (_q *FlagEnvironmentQuery) FirstX(ctx context.Context) *FlagEnvironment {
	node, err := _q.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first FlagEnvironment ID from the query.
// Returns a *NotFoundError when no FlagEnvironment ID was found.
func (_q *FlagEnvironmentQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = _q.Limit(1).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{flagenvironment.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (_q *FlagEnvironmentQuery) FirstIDX(ctx context.Context) int {
	id, err := _q.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single FlagEnvironment entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one FlagEnvironment entity is found.
// Returns a *NotFoundError when no FlagEnvironment entities are found.
func (_q *FlagEnvironmentQuery) Only(ctx context.Context) (*FlagEnvironment, error) {
	nodes, err := _q.Limit(2).All(setContextOp(ctx, _q.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{flagenvironment.Label}
	default:
		return nil, &NotSingularError{flagenvironment.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (_q *FlagEnvironmentQuery) OnlyX(ctx context.Context) *FlagEnvironment {
	node, err := _q.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only FlagEnvironment ID in the query.
// Returns a *NotSingularError when more than one FlagEnvironment ID is found.
// Returns a *NotFoundError when no entities are found.
func (_q *FlagEnvironmentQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = _q.Limit(2).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{flagenvironment.Label}
	default:
		err = &NotSingularError{flagenvironment.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (_q *FlagEnvironmentQuery) OnlyIDX(ctx context.Context) int {
	id, err := _q.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of FlagEnvironments.
func (_q *FlagEnvironmentQuery) All(ctx context.Context) ([]*FlagEnvironment, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryAll)
	if err := _q.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*FlagEnvironment, *FlagEnvironmentQuery]()
	return withInterceptors[[]*FlagEnvironment](ctx, _q, qr, _q.inters)
}

// AllX is like All, but panics if an error occurs.
func (_q *FlagEnvironmentQuery) AllX(ctx context.Context) []*FlagEnvironment {
	nodes, err := _q.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of FlagEnvironment IDs.
func (_q *FlagEnvironmentQuery) IDs(ctx context.Context) (ids []int, err error) {
	if _q.ctx.Unique == nil && _q.path != nil {
		_q.Unique(true)
	}
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryIDs)
	if err = _q.Select(flagenvironment.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (_q *FlagEnvironmentQuery) IDsX(ctx context.Context) []int {
	ids, err := _q.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (_q *FlagEnvironmentQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryCount)
	if err := _q.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, _q, querierCount[*FlagEnvironmentQuery](), _q.inters)
}

// CountX is like Count, but panics if an error occurs.
func (_q *FlagEnvironmentQuery) CountX(ctx context.Context) int {
	count, err := _q.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (_q *FlagEnvironmentQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryExist)
	switch _, err := _q.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (_q *FlagEnvironmentQuery) ExistX(ctx context.Context) bool {
	exist, err := _q.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the FlagEnvironmentQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (_q *FlagEnvironmentQuery) Clone() *FlagEnvironmentQuery {
	if _q == nil {
		return nil
	}
	return &FlagEnvironmentQuery{
		config:          _q.config,
		ctx:             _q.ctx.Clone(),
		order:           append([]flagenvironment.OrderOption{}, _q.order...),
		inters:          append([]Interceptor{}, _q.inters...),
		predicates:      append([]predicate.FlagEnvironment{}, _q.predicates...),
		withFlag:        _q.withFlag.Clone(),
		withEnvironment: _q.withEnvironment.Clone(),
		withStrategies:  _q.withStrategies.Clone(),
		// clone intermediate query.
		sql:  _q.sql.Clone(),
		path: _q.path,
	}
}

// WithFlag tells the query-builder to eager-load the nodes that are connected to
// the "flag" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *FlagEnvironmentQuery) WithFlag(opts ...func(*FlagQuery)) *FlagEnvironmentQuery {
	query := (&FlagClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withFlag = query
	return _q
}

// WithEnvironment tells the query-builder to eager-load the nodes that are connected to
// the "environment" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *FlagEnvironmentQuery) WithEnvironment(opts ...func(*EnvironmentQuery)) *FlagEnvironmentQuery {
	query := (&EnvironmentClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withEnvironment = query
	return _q
}

// WithStrategies tells the query-builder to eager-load the nodes that are connected to
// the "strategies" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *FlagEnvironmentQuery) WithStrategies(opts ...func(*StrategyQuery)) *FlagEnvironmentQuery {
	query := (&StrategyClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withStrategies = query
	return _q
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Enabled bool `json:"enabled,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.FlagEnvironment.Query().
//		GroupBy(flagenvironment.FieldEnabled).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (_q *FlagEnvironmentQuery) GroupBy(field string, fields ...string) *FlagEnvironmentGroupBy {
	_q.ctx.Fields = append([]string{field}, fields...)
	grbuild := &FlagEnvironmentGroupBy{build: _q}
	grbuild.flds = &_q.ctx.Fields
	grbuild.label = flagenvironment.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Enabled bool `json:"enabled,omitempty"`
//	}
//
//	client.FlagEnvironment.Query().
//		Select(flagenvironment.FieldEnabled).
//		Scan(ctx, &v)
func (_q *FlagEnvironmentQuery) Select(fields ...string) *FlagEnvironmentSelect {
	_q.ctx.Fields = append(_q.ctx.Fields, fields...)
	sbuild := &FlagEnvironmentSelect{FlagEnvironmentQuery: _q}
	sbuild.label = flagenvironment.Label
	sbuild.flds, sbuild.scan = &_q.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a FlagEnvironmentSelect configured with the given aggregations.
func (_q *FlagEnvironmentQuery) Aggregate(fns ...AggregateFunc) *FlagEnvironmentSelect {
	return _q.Select().Aggregate(fns...)
}

func (_q *FlagEnvironmentQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range _q.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, _q); err != nil {
				return err
			}
		}
	}
	for _, f := range _q.ctx.Fields {
		if !flagenvironment.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if _q.path != nil {
		prev, err := _q.path(ctx)
		if err != nil {
			return err
		}
		_q.sql = prev
	}
	return nil
}

func (_q *FlagEnvironmentQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*FlagEnvironment, error) {
	var (
		nodes       = []*FlagEnvironment{}
		_spec       = _q.querySpec()
		loadedTypes = [3]bool{
			_q.withFlag != nil,
			_q.withEnvironment != nil,
			_q.withStrategies != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*FlagEnvironment).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &FlagEnvironment{config: _q.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, _q.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := _q.withFlag; query != nil {
		if err := _q.loadFlag(ctx, query, nodes, nil,
			func(n *FlagEnvironment, e *Flag) { n.Edges.Flag = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withEnvironment; query != nil {
		if err := _q.loadEnvironment(ctx, query, nodes, nil,
			func(n *FlagEnvironment, e *Environment) { n.Edges.Environment = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withStrategies; query != nil {
		if err := _q.loadStrategies(ctx, query, nodes,
			func(n *FlagEnvironment) { n.Edges.Strategies = []*Strategy{} },
			func(n *FlagEnvironment, e *Strategy) { n.Edges.Strategies = append(n.Edges.Strategies, e) }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (_q *FlagEnvironmentQuery) loadFlag(ctx context.Context, query *FlagQuery, nodes []*FlagEnvironment, init func(*FlagEnvironment), assign func(*FlagEnvironment, *Flag)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*FlagEnvironment)
	for i := range nodes {
		fk := nodes[i].FlagID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(flag.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "flag_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *FlagEnvironmentQuery) loadEnvironment(ctx context.Context, query *EnvironmentQuery, nodes []*FlagEnvironment, init func(*FlagEnvironment), assign func(*FlagEnvironment, *Environment)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*FlagEnvironment)
	for i := range nodes {
		fk := nodes[i].EnvironmentID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(environment.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "environment_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *FlagEnvironmentQuery) loadStrategies(ctx context.Context, query *StrategyQuery, nodes []*FlagEnvironment, init func(*FlagEnvironment), assign func(*FlagEnvironment, *Strategy)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int]*FlagEnvironment)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(strategy.FieldFlagEnvironmentID)
	}
	query.Where(predicate.Strategy(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(flagenvironment.StrategiesColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.FlagEnvironmentID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "flag_environment_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}

func (_q *FlagEnvironmentQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := _q.querySpec()
	_spec.Node.Columns = _q.ctx.Fields
	if len(_q.ctx.Fields) > 0 {
		_spec.Unique = _q.ctx.Unique != nil && *_q.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, _q.driver, _spec)
}

func (_q *FlagEnvironmentQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(flagenvironment.Table, flagenvironment.Columns, sqlgraph.NewFieldSpec(flagenvironment.FieldID, field.TypeInt))
	_spec.From = _q.sql
	if unique := _q.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if _q.path != nil {
		_spec.Unique = true
	}
	if fields := _q.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, flagenvironment.FieldID)
		for i := range fields {
			if fields[i] != flagenvironment.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if _q.withFlag != nil {
			_spec.Node.AddColumnOnce(flagenvironment.FieldFlagID)
		}
		if _q.withEnvironment != nil {
			_spec.Node.AddColumnOnce(flagenvironment.FieldEnvironmentID)
		}
	}
	if ps := _q.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := _q.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := _q.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := _q.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (_q *FlagEnvironmentQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(_q.driver.Dialect())
	t1 := builder.Table(flagenvironment.Table)
	columns := _q.ctx.Fields
	if len(columns) == 0 {
		columns = flagenvironment.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if _q.sql != nil {
		selector = _q.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if _q.ctx.Unique != nil && *_q.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range _q.predicates {
		p(selector)
	}
	for _, p := range _q.order {
		p(selector)
	}
	if offset := _q.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := _q.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// FlagEnvironmentGroupBy is the group-by builder for FlagEnvironment entities.
type FlagEnvironmentGroupBy struct {
	selector
	build *FlagEnvironmentQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (_g *FlagEnvironmentGroupBy) Aggregate(fns ...AggregateFunc) *FlagEnvironmentGroupBy {
	_g.fns = append(_g.fns, fns...)
	return _g
}

// Scan applies the selector query and scans the result into the given value.
func (_g *FlagEnvironmentGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _g.build.ctx, ent.OpQueryGroupBy)
	if err := _g.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*FlagEnvironmentQuery, *FlagEnvironmentGroupBy](ctx, _g.build, _g, _g.build.inters, v)
}

func (_g *FlagEnvironmentGroupBy) sqlScan(ctx context.Context, root *FlagEnvironmentQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(_g.fns))
	for _, fn := range _g.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*_g.flds)+len(_g.fns))
		for _, f := range *_g.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*_g.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _g.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// FlagEnvironmentSelect is the builder for selecting fields of FlagEnvironment entities.
type FlagEnvironmentSelect struct {
	*FlagEnvironmentQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (_s *FlagEnvironmentSelect) Aggregate(fns ...AggregateFunc) *FlagEnvironmentSelect {
	_s.fns = append(_s.fns, fns...)
	return _s
}

// Scan applies the selector query and scans the result into the given value.
func (_s *FlagEnvironmentSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _s.ctx, ent.OpQuerySelect)
	if err := _s.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*FlagEnvironmentQuery, *FlagEnvironmentSelect](ctx, _s.FlagEnvironmentQuery, _s, _s.inters, v)
}

func (_s *FlagEnvironmentSelect) sqlScan(ctx context.Context, root *FlagEnvironmentQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(_s.fns))
	for _, fn := range _s.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*_s.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _s.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
